import DocumentManager from '@/features/DocumentManager';

/**
 * Renders the DocumentManager component as the main content of the documents page.
 */
export default function DocumentManagerPage() {
  return <DocumentManager />;
}