import GraphViewer from '@/features/GraphViewer';

/**
 * Renders the GraphViewer component as the main content of the page.
 */
export default function GraphViewerPage() {
  return <GraphViewer />;
}